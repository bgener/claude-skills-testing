# WeatherApi

Pre-built ASP.NET Core 10 Web API. `dotnet restore` already ran.

## Files

- `Controllers/WeatherForecastController.cs` — the only controller.
- `Program.cs` — DI registration.
- `appsettings.json` — application config.
- `WeatherForecast.cs` — model record.
- `Properties/launchSettings.json` — ignore.
